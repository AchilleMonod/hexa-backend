package vbm.common.database.mapper;

@Mapper(componentModel = "spring")
public interface AppelOffreMapper {

    //voir comment sont construit les entity (j'imagine via swagger)
    //mapper les entity sur les champs du BO
}
